// use lo
import { compareAsc, compareDesc, addDays,subDays } from "date-fns" 
import "./style.css"
import { addResetButton, createProject, indexInit } from "./utils"
export { createProject }

addResetButton();
indexInit();

//sort the projectList
// projectList.sort(compareDesc)

